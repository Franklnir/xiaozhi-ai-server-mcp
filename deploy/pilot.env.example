# Pilot (single mini-PC)
APP_ENV=development
APP_DEBUG=true

# One box: API + MCP worker together
MCP_SUPERVISOR_ENABLED=true

# Cleanup (keep last 30 days)
CLEANUP_ENABLED=true
CLEANUP_DAYS=30
CLEANUP_INTERVAL_MINUTES=360

# Security (pilot)
COOKIE_SECURE=false
CORS_ALLOW_ORIGINS=*
CORS_ALLOW_CREDENTIALS=false
